---
import logo from "../assets/logo.png";

const navItems = [
  { label: "Realizacje", href: "/#realizacje" },
  { label: "Kontakt", href: "/#kontakt" },
  { label: "Wycena", href: "/#wycena", primary: true },
  { label: "Umów konsultacje", href: "/#konsultacja", primary: true },
];
---

<header
  data-header
  class="fixed top-0 z-50 flex w-full items-center justify-between px-4 py-2 text-sm uppercase tracking-[0.2em] text-white transition-all duration-300 ease-out"
>
  <a
    href="/"
    aria-label="BT Wood Studio - strona główna"
    class="flex h-12 items-center gap-2 tracking-tight"
  >
    <img
      src={logo.src}
      alt="BT Wood Studio logo"
      class="h-8 w-8 rounded-sm object-cover grayscale"
    />
    <span data-logo-text class="text-sm tracking-[0.35em] text-stone-100"
      >WOOD STUDIO</span
    >
  </a>

  <nav aria-label="Main navigation" class="hidden items-center gap-4 md:flex">
    {
      navItems.map((item) => (
        <a
          href={item.href}
          data-nav-link
          data-primary={item.primary ? "true" : "false"}
          class:list={[
            "transition duration-150 ease-out text-[0.7rem]",
            item.primary
              ? "rounded-sm bg-stone-600 px-4 py-2 tracking-[0.2em] text-stone-50 hover:bg-stone-500"
              : "hover:text-stone-300",
          ]}
        >
          {item.label}
        </a>
      ))
    }
  </nav>

  <button
    type="button"
    aria-label="Open mobile navigation menu"
    aria-expanded="false"
    aria-controls="mobile-navigation-panel"
    data-menu-button
    class="uppercase transition-colors duration-300 md:hidden"
  >
    <span>Menu</span>
  </button>
</header>

<style is:global>
  [data-header] {
    border-bottom: 1px solid transparent;
    backdrop-filter: blur(0px);
  }

  [data-header].is-scrolled {
    border-bottom-color: rgba(214, 211, 209, 0.8);
    background: rgba(255, 255, 255);
    box-shadow: 0 10px 30px rgba(28, 25, 23, 0.08);
    backdrop-filter: blur(12px);
    padding-top: 0.7rem;
    padding-bottom: 0.7rem;
    color: rgb(28 25 23);
  }

  [data-header].is-scrolled [data-logo-text] {
    color: rgb(28 25 23);
  }

  [data-header].is-scrolled [data-menu-button] {
    color: rgb(28 25 23);
  }

  [data-header].is-scrolled [data-nav-link][data-primary="false"] {
    color: rgb(28 25 23);
  }

  [data-header].is-scrolled [data-nav-link][data-primary="false"]:hover {
    color: rgb(68 64 60);
  }

  [data-header].is-scrolled [data-nav-link][data-primary="true"] {
    background: rgb(41 37 36);
    color: rgb(250 250 249);
  }

  [data-header].is-scrolled [data-nav-link][data-primary="true"]:hover {
    background: rgb(68 64 60);
  }
</style>

<div
  data-mobile-backdrop
  class="pointer-events-none fixed inset-0 z-40 bg-stone-950/60 opacity-0 transition-opacity duration-200 md:hidden"
  aria-hidden="true"
>
</div>

<aside
  id="mobile-navigation-panel"
  data-mobile-panel
  class="fixed right-0 top-0 z-50 flex h-full w-full max-w-sm translate-x-full flex-col border-l border-white/10 bg-stone-950/95 p-6 pt-20 shadow-2xl transition-transform duration-300 md:hidden"
  aria-label="Mobile navigation"
>
  <nav aria-label="Mobile navigation" class="mt-4">
    <ul class="space-y-2">
      {
        navItems.map((item) => (
          <li>
            <a
              href={item.href}
              class:list={[
                "flex items-center justify-between rounded-md px-3 py-3 text-base tracking-[0.2em] text-stone-100 transition hover:bg-white/5",
                item.primary && "bg-stone-800/80",
              ]}
            >
              <span>{item.label}</span>
              <span aria-hidden="true">→</span>
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</aside>

<script>
  const header = document.querySelector("[data-header]");
  const menuButton = document.querySelector("[data-menu-button]");
  const mobilePanel = document.querySelector("[data-mobile-panel]");
  const mobileBackdrop = document.querySelector("[data-mobile-backdrop]");

  const updateHeaderState = () => {
    if (!header) return;
    const scrolled = window.scrollY >= 40;
    header.classList.toggle("is-scrolled", scrolled);
  };

  window.addEventListener("scroll", updateHeaderState, { passive: true });
  updateHeaderState();

  if (menuButton && mobilePanel && mobileBackdrop) {
    const setMenuState = (isOpen: boolean) => {
      const ariaValue = String(isOpen);
      menuButton.setAttribute("aria-expanded", ariaValue);
      menuButton.setAttribute(
        "aria-label",
        isOpen ? "Close mobile navigation menu" : "Open mobile navigation menu",
      );
      mobilePanel.classList.toggle("translate-x-full", !isOpen);
      mobileBackdrop.classList.toggle("opacity-0", !isOpen);
      mobileBackdrop.classList.toggle("pointer-events-none", !isOpen);
      document.body.classList.toggle("overflow-hidden", isOpen);
    };

    const closeMenu = () => setMenuState(false);
    const openMenu = () => setMenuState(true);

    menuButton.addEventListener("click", () => {
      const isOpen = menuButton.getAttribute("aria-expanded") === "true";
      if (isOpen) {
        closeMenu();
      } else {
        openMenu();
      }
    });

    mobileBackdrop.addEventListener("click", closeMenu);

    mobilePanel.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", closeMenu);
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") {
        closeMenu();
      }
    });
  }
</script>
